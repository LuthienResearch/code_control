# ----------------- CLIENT CONFIGURATION -----------------
# These settings are for clients connecting to the LCC proxy

# LCC proxy URL
LCC_URL=http://localhost:8000

# Client identifier (optional, for future auth)
CLIENT_API_KEY=client_1234

# ----------------- SERVER CONFIGURATION -----------------
# These settings are used by the LCC server (not needed by clients)

# API Keys for providers
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here

# Advanced configuration
# LCC_TARGET_URL=https://api.anthropic.com/v1  # Override target API URL
# LCC_API_KEY=your_key_here                    # Directly set API key instead of using provider variables
# LCC_API_KEY_HEADER=x-api-key                 # For Anthropic (default); use 'Authorization' for OpenAI
# LCC_API_PROVIDER=anthropic                   # Default provider (anthropic or openai)
# LCC_LOG_LEVEL=INFO                           # Logging level